# build_data.py needs nothing beyond the standard library — that is deliberate,
# so anyone can rebuild the app's datasets without a Python environment fight.

# fetch_dem.py and fetch_photos.py — decode terrain tiles and resize photos
Pillow>=10.0.0

# ingest_hdb.py — regenerate the HDB block list (roughly annual)
requests>=2.31.0

# ingest_hdb.py and fetch_photos.py — read credentials from .env.local
python-dotenv>=1.0.0

# scrape_routes_playwright.py — dynamic browser scraping and GPX generation
playwright>=1.40.0
gpxpy>=1.6.0

# api_route_import.py — backend API route importer
httpx>=0.27.0
tenacity>=8.0.0
